# gmail-filter-cleanup

## gstack

Use the `/browse` skill from gstack for all web browsing. Never use `mcp__claude-in-chrome__*` tools.

Available gstack skills:
- `/office-hours` — open-ended design/eng/product discussion
- `/plan-ceo-review` — prep a plan for CEO review
- `/plan-eng-review` — prep a plan for engineering review
- `/plan-design-review` — prep a plan for design review
- `/design-consultation` — consult on design decisions
- `/review` — code review
- `/ship` — ship a feature end-to-end
- `/browse` — web browsing (use this instead of any MCP browser tools)
- `/qa` — full QA pass
- `/qa-only` — QA without shipping
- `/design-review` — review designs
- `/setup-browser-cookies` — configure browser auth cookies
- `/retro` — run a retrospective
- `/debug` — systematic debugging session
- `/document-release` — document a release

Teammates: run `.claude/skills/gstack/setup` after cloning (requires `bun`).
